{
    "name": "logseqserver",
    "version": "0.1.2",
    "description": "Zotero Local Server for Logseq integration",
    "private": true,
    "repository": "https://github.com/mathcals/zotero-logseqserver",
    "author": "mathcals@github.com",
    "license": "AGPL",
    "scripts": {
        "build": "esbuild src/bootstrap.ts --bundle --outfile=bootstrap.js",
        "watch": "esbuild src/bootstrap.ts --bundle --outfile=bootstrap.js --watch",
        "lint": "tsc -noEmit && eslint src"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^5.10.2",
        "@typescript-eslint/parser": "^5.10.2",
        "esbuild": "^0.14.15",
        "eslint": "^8.8.0",
        "typescript": "^4.5.5"
    },
    "dependencies": {
        "@types/node": "^20.6.0",
        "fs-extra": "^11.1.1"
    }
}
